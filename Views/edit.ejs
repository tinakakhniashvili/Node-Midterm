<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>Edit Item</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
  </head>
  <body class="bg-light">
    <div class="container py-5">
      <h1 class="mb-4">✏️ Edit Item</h1>

      <form action="/edit/<%= item._id %>" method="POST" class="row g-3">
        <div class="col-md-6">
          <input
            type="text"
            name="name"
            value="<%= item.name %>"
            class="form-control"
            required
          />
        </div>
        <div class="col-md-6">
          <input
            type="number"
            name="quantity"
            value="<%= item.quantity %>"
            class="form-control"
            required
          />
        </div>
        <div class="col-12 d-grid">
          <button type="submit" class="btn btn-primary">💾 Update</button>
        </div>
      </form>

      <div class="mt-4">
        <a href="/" class="btn btn-secondary">⬅️ Back to Home</a>
      </div>
    </div>
  </body>
</html>
